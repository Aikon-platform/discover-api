"""
This API is separated into several modules :

- `shared` : contains shared functions and classes used by the other modules
- `config` : contains the configuration files of the API

And then, several modules for the different parts of the API.
"""